pipeline{
  stages{
     stage("docker build"){
        steps{
           sh "git rev-parse --short HEAD > .git/commit-id"
           def commit_id = readFile('.git/commit-id').trim()
           echo "${env.BUILD_ID}-${commit_id}"
        }
     }
  }
}
